from automatConstructor import Automat

class wordsAlgos:

    automat = None

    def __init__(self, automat):
        self.automat = automat



    def findWords(self, startingString):
        return
        #1. find state
        #2. find all next exit state
        